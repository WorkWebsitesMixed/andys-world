---
courseId: python
sprint: 1
week: 3
title: "Making Decisions"
topic: "Conditionals"
coreSkill: "if / elif / else, random.randint()"
payoff: "Build a Magic 8-Ball that gives a different answer every time you shake it"
learningGoals:
  - "Write a correct if / elif / else chain"
  - "Use random.randint() and random.choice() to introduce randomness"
  - "Nest a conditional inside another"
  - "Use or and and to combine conditions"
files:
  - filename: "S1_W3_template.py"
    label: "Starter template"
  - filename: "S1_W3_exercises.py"
    label: "Exercises"
---

The computer needs to make decisions. This session gives it that ability. You will build a Magic 8-Ball: ask it anything, get a different answer every time. By the end you will understand why `if`/`elif`/`else` is the backbone of almost every program ever written.
